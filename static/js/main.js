document.addEventListener('DOMContentLoaded', () => { document.querySelectorAll('.quantity-control').forEach(control => { const input=control.querySelector('input[type="number"]'); control.querySelector('.quantity-minus')?.addEventListener('click',()=>input.stepDown()); control.querySelector('.quantity-plus')?.addEventListener('click',()=>input.stepUp()); }); document.querySelectorAll('.remove-form').forEach(form=>form.addEventListener('submit',event=>{if(!window.confirm('Are you sure you want to remove this item?'))event.preventDefault();})); window.setTimeout(()=>document.querySelectorAll('.alert').forEach(alert=>bootstrap.Alert.getOrCreateInstance(alert).close()),5000); });
